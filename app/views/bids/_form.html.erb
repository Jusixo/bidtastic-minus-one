<%= form_for([@item.auction, @item, @item.bids.build]) do |f| %>
    <%= f.label :bid_amount, "Bid Amount", class: "col-xs-4" %>
    <%= f.number_field :bid_amount, value: "#{@item.next_minimum_bid}", class: "col-xs-4"%>
    <%= f.submit "Place Bid", class: "pull-right"%>
<% end %>
